/**
 * @description
 * The plugin can be configured using the following options:
 */
export interface PluginInitOptions {
    /**
     * Path to Firebase service account JSON file
     * Defaults to 'src/service-account.json'
     */
    serviceAccountPath?: string;
}
